global:
  resolve_timeout: 5m
  smtp_from: 'simao.bmarcos@gmail.com'
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_auth_username: 'simao.bmarcos@gmail.com'
  smtp_auth_password: 'wexgworzpirknlzf'
  smtp_require_tls: true

route:
  receiver: email-alerts
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: email-alerts
    email_configs:
      - to: 'simao.bmarcos@gmail.com'
        send_resolved: true
