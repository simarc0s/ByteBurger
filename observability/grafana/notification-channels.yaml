apiVersion: 1

notifiers:
  - name: Teams
    type: teams
    uid: teams-webhook
    org_id: 1
    is_default: true
    send_reminder: true
    frequency: "1h"
    settings:
      url: "${TEAMS_WEBHOOK_URL}"
    secureSettings:
      url: "${TEAMS_WEBHOOK_URL}"
